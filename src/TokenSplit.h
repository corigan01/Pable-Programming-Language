#ifndef __TOKEN_SPLIT_H
#define __TOKEN_SPLIT_H

#include "base.h"
#include "tokenFunctions.h"
#include "FileIO.h"

class FileTokens : private FileIO {
    public:

        FileTokens();
        ~FileTokens();

        
        
        
 

};





#endif